<%= image_tag @photo.image.url(:medium) %><br>


<%= link_to "Upload New Photo", new_event_photo_path(@event), class: "btn btn-primary btn-sm photoUpload"%>

<%= link_to "PhotoStream", event_photos_path(@event), class: "btn btn-success btn-sm photoUpload" %>

