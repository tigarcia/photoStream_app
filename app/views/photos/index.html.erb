<h1>Photo Index</h1>

<% @photos.each do |photo| %>
	<%= link_to photo.name, photo_path(photo.id) %>
	
<% end %>

<h3>TODO: need to update so that only photos associated with event id are shown.</h3>
